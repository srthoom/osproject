

Diners are allocated tables on first come first served (FCFS) basis. Activity log is generated for the entire duration, the restaurant is open. Diners and Cooks are implemented as JAVA threads and Tables and Machines are treated as resources.
